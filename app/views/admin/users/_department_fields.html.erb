<div class="form-inputs">
  <%= ff.input :user_id, as: :hidden, input_html: { value: @admin_user.id } %>
  <%= ff.input :author_id, as: :select, collection: Author.all.map{|a| [a.name, a.id]}, include_blank: false %> 
  <% if ff.object.persisted? %>
    <%= ff.input :_destroy, as: :boolean %>
  <% end %>
</div>
